# TerraPulse Gallery Images

The portfolio gallery is wired to these filenames:

- `terrapulse-map-light.png`
- `terrapulse-satellite.png`
- `terrapulse-analytics.png`
- `terrapulse-login.png`

The gallery hides missing files automatically, so the page will not show broken thumbnails while screenshots are being added.
